<h1>Ваши папки</h1>
<%= link_to 'Добавить папку', new_manage_folder_path %>

<ul>
  <% @folders.each do |folder| %>
    <li>
      <%= folder.name %>
      <%= link_to 'Удалить', manage_folder_path(folder), method: :delete, data: { confirm: 'Вы уверены?' } %>
    </li>
  <% end %>
</ul>
